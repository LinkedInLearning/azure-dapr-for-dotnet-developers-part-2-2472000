apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wisdompetmedicine-hospital-api-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  rules:
  - http:
      paths:
      - path: /hospital
        pathType: Prefix
        backend:
          service:
            name: hospital
            port:
              number: 80
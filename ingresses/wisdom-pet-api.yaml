apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wisdompetmedicine-pet-api-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  rules:
  - http:
      paths:
      - path: /pet
        pathType: Prefix
        backend:
          service:
            name: pet
            port:
              number: 80